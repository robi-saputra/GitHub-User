<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@android:color/transparent" /> <!-- Background color when pressed -->
            <stroke
                android:width="1dp"
                android:color="@color/colorDefaultPressed" />
            <corners android:radius="50dp" />
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="@android:color/transparent" /> <!-- Default background color -->
            <stroke
                android:width="1dp"
                android:color="@color/colorOnSurfaceSecondary" />
            <corners android:radius="50dp" />
        </shape>
    </item>
</selector>